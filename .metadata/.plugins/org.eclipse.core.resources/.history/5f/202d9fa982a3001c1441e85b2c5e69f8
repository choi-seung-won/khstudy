package day0314;
// 한번에 여러 학생을 관리하는 프로그램.
// 단, 최대 관리 가능한 학생은 5명이다.
// 단, 한번에 한명씩만 입력이 가능한데
// 만약 다섯번째 학생을 입력한 후에
// 또 입력을 선택할 시에는 더이상 입력할 수 없다 라는 메세지만 출력이 되어야 한다.
// 또한 출력 시에는 현재 입력된 학생까지만 출력이 된다.
// 또한 입력 시에 

import java.util.*;

public class Ex04GradeBook2 {
    
    // 전역 상수
    // 1. 관리할 학생의 최대 숫자
    public static final int STUDENT_SIZE = 5;
    // 2. 과목의 숫자
    public static final int SUBJECT_SIZE = 3;
    // 3. 가능한 최소 점수
    public static final int SCORE_MIN = 0;
    // 4. 가능한 최대 점수
    public static final int SCORE_MAX = 100;
    
    // 전역 변수
    // 1. 입력을 처리할 Scanner 클래스 변수
    public static final Scanner scanner = Scanner(System.in);
    // 2. 학생 번호를 저장할 배열
    // 3. 학생 이름을 저장할 배열
    // 4. 학생의 국어점수를 저장할 배열
    // 5. 학생의 영어점수를 저장할 배열
    // 6. 학생의 수학점수를 저장할 배열
    // 7. 각 배열의  현재 입력할 공통 인덱스

    
    
    
}
